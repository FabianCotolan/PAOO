#ifndef FOOTBALLTEAM_H
#define FOOTBALLTEAM_H

#include "FootballPlayer.h"
#include <memory>
#include <mutex>

class FootballTeam {
private:
    std::unique_ptr<FootballPlayer> player;
    std::mutex teamMutex;

public:
    FootballTeam(const std::string &playerName, int goals);
    void usePlayer();
    void incrementPlayerGoals();
};

#endif

